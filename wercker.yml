# vim: set ts=2 sts=2 sw=2 expandtab:
---
box: debian
build:
  steps:
    - arjen/hugo-build:
      version: 0.14
      theme: cactus
      config: config.yaml
      flags: ""

deploy:
  steps:
    - gh-pages:
      token: $GIT_TOKEN
      domain: coxley.com
      basedir: public
